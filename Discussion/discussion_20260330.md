# 2026-03-30 修改簡述（補記事件）

1. 在開始任務對話框新增「補記時長（分鐘）」獨立欄位，放在備註上方，避免一定要用 JSON 才能填 duration。
2. 「只記錄」流程改為優先使用獨立欄位的 duration，仍保留對舊格式備註（JSON / `duration=xx`）的相容解析。
3. 新增 duration 上限：`720` 分鐘（12 小時）。
4. 前端與流程層皆有驗證：
   - 非數字或小於 0 會擋下。
   - 超過上限會擋下並顯示錯誤訊息。
5. 記錄行為維持不變：只在 Log 新增一筆 `action=RECORD`、`state=DONE`，不動 dashboard、不通知 iPhone。
6. 已補測試：`taskFlow.test.ts` 增加
   - 明確欄位優先於備註解析
   - 超過上限時回傳錯誤
